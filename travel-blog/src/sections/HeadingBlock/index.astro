---
import {PortableText} from '@/components';


const { heading } = Astro.props;
---


{heading && Array.isArray(heading) && heading.length > 0 && (
  <div class="text-center text-primary my-8">
    <PortableText portableText={heading} />
  </div>
)}
