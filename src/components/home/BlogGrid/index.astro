---
// Components
import BlogCard from '@/components/home/BlogCard/index.astro';

// Types
import type { Blog } from '@/types/blog';

interface BlogCardProps {
  posts: Blog[];
  column: string;
}
const { posts, column = '3' } = Astro.props as BlogCardProps;
---

<div class={`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-${column} gap-10`}>
  {
    posts.map((post) => (
      <BlogCard {...post} className="h-[250px] md:h-[300px] xl:h-[400px]" />
    ))
  }
</div>
