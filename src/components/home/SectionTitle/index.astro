---
const { id, title, subTitle } = Astro.props;
---

<div class="text-center text-primary my-8" id={id}>
  <h2 class="text-xl md:text-2xl font-bold">{title}</h2>
  {subTitle && <p class="mt-2">{subTitle}</p>}
</div>
