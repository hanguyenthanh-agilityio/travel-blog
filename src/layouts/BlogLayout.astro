---
import Header from '@/components/common/Header/index.astro';
import Footer from '@/components/common/Footer/index.astro';
import MainHead from './MainHead.astro';
import '../styles/globals.css';

const seo = Astro.props.seo || {};

const {
  title = seo.title || 'My Astro Blog',
  description = seo.description || 'My musings about the Astro framework',
  image = seo.image,
  url = seo.url,
  noindex = seo.noindex ?? false,
} = Astro.props;
---

<html lang="en">
  <MainHead {title} {description} {image} {url} {noindex} />
  <body>
    <Header />
    <main class="flex-1 container mx-auto px-4 py-8">
      <slot />
    </main>
    <Footer />
  </body>
</html>
